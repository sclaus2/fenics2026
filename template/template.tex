\documentclass{article}
\PassOptionsToPackage{short, nodayofweek}{datetime}

\input{fenicsconf.def}
\makeatletter
\let\@seccntformat\@gobble
\makeatother

\usepackage{afterpage}
\usepackage{longtable}

[-IMPORTS-]

% colors for hyperlinks
\hypersetup{colorlinks=true, allcolors=blue}
\hypersetup{
pdftitle={\@title},
pdfsubject={[-doc.description-]},
pdfauthor={\@author},
pdfkeywords={[-doc.tags-]}
}

\usepackage{fenicsconf}
% You should have an imports section
[- IMPORTS -]

\fancypagestyle{frontmattersecondpage}{
  \fancyhf{}
  \renewcommand{\headrulewidth}{0.4pt}
  \rhead{\scshape \footnotesize \headeright}
  \chead{\shorttitle}
}


\title{[-doc.title-]}

\author{[# for author in doc.authors -#]
[#- if author.email -#]\textbf{[-author.name-]}[#- else -#][-author.name-][#- endif -#][# if doc.affiliations.length > 1 and author.affiliations #]\textsuperscript{[-author.affiliations|join(",", "index") --]}[# endif #][# if not loop.last #], [# endif #]
[#- endfor -#]
[# if doc.affiliations #]\\[0.5em]
[# for affiliation in doc.affiliations #]
[# if doc.affiliations.length == 1 #]
[-affiliation.value.name-]\\
[# else #]
\textsuperscript{[-affiliation.index-]} [-affiliation.value.name-]\\
[# endif #]
[# endfor #]
[# endif #]
}


\newdate{articleDate}{[-doc.date.day-]}{[-doc.date.month-]}{[-doc.date.year-]}
\date{\displaydate{articleDate}}

\begin{document}
\maketitle
[# if doc.title == "FEniCS Conference 2026 Book of Abstracts" #]
\afterpage{\thispagestyle{frontmattersecondpage}}
[# else #]
\afterpage{\thispagestyle{empty}}
[# endif #]

[-CONTENT-]

\end{document}
